import 'package:flutter/material.dart';

class DailyEventsList extends StatelessWidget {
  const DailyEventsList({super.key});

  @override
  PreferredSizeWidget build(BuildContext context) {
    
    return Column();
  }
}